<!DOCTYPE html>
<html>
  <head>
    <title>Notesapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= favicon_link_tag 'logo.ico' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'wind', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <p class="flex place-content-center leading-8 font-regular bg-green-500"><%= notice %></p>
    <p class="flex place-content-center leading-8 font-regular bg-red-500"><%= alert %></p>
    <% if controller_name == "registrations"  || controller_name == "sessions" || controller_name == "passwords"%>
    <%else %>
      <%= render 'layouts/header' %>
    <% end %>
    <%= yield %>
  </body>
</html>
