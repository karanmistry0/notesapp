<nav class="bg-white shadow">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-20 items-center justify-between">
      <div class="flex items-center">
        <div >
          <%= link_to "Notes", root_path, class:"flex flex-shrink-0 font-medium text-3xl flex-shrink-0" %>
        </div>
      </div>
      <div class="hidden sm:ml-6 sm:flex sm:items-center gap-4">
        <% if user_signed_in? %>
          <%= link_to 'Add Notes', new_note_path,  class:"border border-black rounded-md px-6 py-2  hover:-ml-1 hover:-mt-1 hover:border-b-4 hover:border-r-4"%>
          <%= button_to 'Log out', destroy_user_session_path, method: :delete, class:"border border-black rounded-md px-6 py-2  hover:-ml-1 hover:-mt-1 hover:border-b-4 hover:border-r-4"%>
        <% else %>
          <%= link_to 'Log in',user_session_path,class:"border border-black rounded-md px-6 py-2  hover:-ml-1 hover:-mt-1 hover:border-b-4 hover:border-r-4"%>
        <% end %>

      </div>
    </div>
  </div>
</nav>